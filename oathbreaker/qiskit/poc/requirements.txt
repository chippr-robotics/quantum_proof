qiskit>=2.1,<3
qiskit-aer>=0.15
qiskit-ibm-runtime>=0.30
numpy>=1.23
# TKET integration (Commit 1): pytket-qiskit's bidirectional Qiskit
# bridge requires pytket >= 2.0 and pytket-qiskit >= 0.50 for Qiskit 2.x
# support. These are CI-required; the test_compilers.py correctness
# sweep is a blocking job.
pytket>=2.0,<3
pytket-qiskit>=0.50
